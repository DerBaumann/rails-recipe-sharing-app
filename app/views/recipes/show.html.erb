<% if notice %>
  <div role="alert" class="alert alert-success mb-4">
    <%= heroicon "check-badge", options: { class: "w-6" } %>
    <span><%= notice %></span>
  </div>
<% end %>

<h1 class="h1"><%= @recipe.title %></h1>

<p>
  <%= render "favourites/toggle_button", recipe: @recipe %>
</p>

<p><%= @recipe.prep_time_minutes %>min. Kochzeit</p>

<ul class="list list-disc">
  <% @recipe.ingredients.each do |ingredient| %>
    <li class="list-item">
      <%= ingredient.amount %>
      <%= ingredient.unit %>
      <%= ingredient.name %>
    </li>
  <% end %>
</ul>

<div class="prose max-w-none">
  <%= simple_format(@recipe.instructions) %>
</div>

<div class="flex gap-4">
  <%= link_to "Back to recipes", recipes_path, class: "btn btn-neutral" %>
  <% if @recipe.owned_by?(Current.user) %>
    <%= button_to "Destroy this recipe",
    @recipe,
    method: :delete,
    class: "btn btn-error" %>
    <%= link_to "Edit this recipe", edit_recipe_path(@recipe), class: "btn btn-primary" %>
  <% end %>
</div>

<hr class="my-8"/>

<section class="mt-8">
  <%= render "comments/header", recipe: @recipe %>

  <%= render "comments/form_section", recipe: @recipe %>

  <div id="comments_list">
    <% @recipe.comments.order(created_at: :desc).each do |comment| %>
      <%= render "comments/comment", comment: comment, recipe: @recipe %>
    <% end %>
  </div>
</section>
